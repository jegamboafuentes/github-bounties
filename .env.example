# Copy to .env locally. Never commit real values.

# --- GitHub App webhooks (V0-B) ---
# Required. GitHub App → Webhook secret (HMAC).
GITHUB_WEBHOOK_SECRET=

# Optional. Numeric App ID. Not used by the webhook stub; needed later for API fetches
# (merge commit message, GraphQL closingIssuesReferences).
GITHUB_APP_ID=

# Optional. Path to the App private key PEM (gitignored). Not used by this spike.
# GITHUB_APP_PRIVATE_KEY_PATH=./github-bounties-staging.pem

PORT=3000
HOST=127.0.0.1

# Public origin used in docs for staging callback / webhook / setup URLs.
PUBLIC_BASE_URL=https://github-bounties-staging.example.com

# File-backed delivery-id store (idempotency). Directory is created on start.
DELIVERY_STORE_PATH=.data/deliveries.json

# --- CDP / x402 (V0-A) ---
# GCP Secret Manager names match these env vars (project github-bounties / 133702056111).
# See docs/adr/0001-cdp-x402-wallets.md

# Required for any CDP wallet or x402 facilitator call
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=

# Optional but recommended
CDP_PROJECT_ID=
CDP_CLIENT_API_KEY=

# Sandbox only. Dry-run refuses anything other than base-sepolia.
CDP_NETWORK=base-sepolia
CDP_DRY_RUN=1
